# SAFE MODIFICATION MAP — GESTALT v0.029

## SAFE
Colors, gradients, shadows, borders, typography, spacing, responsive breakpoints, copy without changing DOM ids, animation timing/easing without changing transform semantics.

## CAUTION
Queue ordering/length, `BALANCE`, `answerWord()`, counters/status mapping, topic filtering, service worker cache policy, Telegram CloudStorage chunking.

## CRITICAL
Save schema/migration, review/mastery schema, Telegram sync schema, card face DOM/3D transforms, canonical vocabulary schema/content, Bot API/webhook secrets.

## Verification
Run `npm test`, `npm run check`, `npm run platform-check`. For card changes verify front→back, back→front, rapid/double clicks, swipe, keyboard, mobile and desktop.
